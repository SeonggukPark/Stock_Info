[project]
name = "stock-info"
version = "0.1.0"
description = "Daily US stock report generator with HTML email and PDF output."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
stock-info = "stock_info.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
